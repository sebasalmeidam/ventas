<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <title>Ventas</title>
    <meta name="description"
      content="<%= !@description.nil? ? @description : 'Solución para ventas en línea.' %>"
    />

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <meta property="og:title"
      content="<%= @title.present? ? "#{@title}" : 'Ventas en línea' %>" />
    <meta property="og:description"
      content="<%= @description.present? ? @description : 'Facilita tus ventas en línea' %>" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="" />
    <meta property="og:site_name" content="" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="fb:admins" content="" />

    <!-- WEB FONTS : use %7C instead of | (pipe) -->
		<script type="text/javascript">
			
			WebFontConfig = {
		    google: {families: ['Roboto:300,400,400italic,600,700,800', 'Open+Sans:300,400,600', 'Raleway:300,400,500,600,700', 'Lato:300,400,400italic,600,700', 'Poppins:300,400,400italic,600,700,800']}
		  };
		  (function() {
		    var wf = document.createElement('script');
		    wf.src = 'https://ajax.googleapis.com/ajax/libs/webfont/1.6.26/webfont.js';
		    wf.type = 'text/javascript';
		    wf.async = 'true';
		    var s = document.getElementsByTagName('script')[0];
		    s.parentNode.insertBefore(wf, s);
		  })(); 
		</script>
    
    <%# favicon_link_tag %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body class="mdb-skin">
    <header>
      <%= render partial: 'layouts/shared/back_sidebar' %>
      <%= render partial: 'layouts/shared/back_header' %>
    </header>
    <main id="content" class="content" style="padding-top: 70px;">
      <%= yield %>
    </main>
    <footer>
      
    </footer>
  </body>
</html>
