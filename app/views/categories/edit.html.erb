<div class="container">
  <h1>Editar Categoría</h1>
  <div class="row">
    <div class="col-12">
      <%= link_to 'Regresar', categories_path(current_user), class: 'btn btn-default btn-sm ml-0' %>
    </div>
  </div>

  <section>
    <div class="row">
      <div class="col-12 col-lg-4">
        <%= form_with model: @category, local: true do |f| %>
          <%= render partial: 'categories/categories_form', locals: {f: f} %>
        <% end %>
      </div>
    </div>
  </section>
</div>